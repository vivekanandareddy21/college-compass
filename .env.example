# Database connection string for PostgreSQL
DATABASE_URL="postgresql://postgres:172516@localhost:5432/college_compass?schema=public"

# Secret key for signing JWT tokens (should be a long random string in production)
JWT_SECRET="your_jwt_secret_key_here_minimum_32_characters"

# Next.js Application Port (Optional, defaults to 3000)
PORT=3000
